pii_config:
    language: 'en'
    supported_entities:
    - PERSON
    - ADDRESS
    anonymize:
        #type: 'replace'
        #new_value: ABC
        action: 'mask'
        chars_to_mask: 40
        masking_char: '*'

        #type: 'hash'
        #hash_type: 'sha256'

        #type: 'redact'
