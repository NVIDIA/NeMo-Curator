comment: false
coverage:
  status:
    patch:
      default:
        target: 80%
        threshold: 0%
        base: auto
        if_ci_failed: error
    project:
      default:
        target: 80%
        threshold: 0%
        base: auto
        if_ci_failed: success
      branches:
        main:
          target: 80%
          threshold: 0%
          base: auto
          if_ci_failed: error
fixes:
  - "/opt/NeMo-Curator/::"
